target/
# One working tree per agent, made by `wsp checkout`. Nested under the root so
# project resolution still places a pane standing in one; see src/cmd_checkout.rs.
.worktrees/

# Claude Code makes its own worktrees here if it is ever allowed to. wsp cannot
# see one — see agent-010 and .claude/settings.json.
.claude/worktrees/
