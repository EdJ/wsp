[package]
name = "wsp"
version = "0.1.0"
edition = "2021"
description = "Workspace and task control plane for herdr"

[[bin]]
name = "wsp"
path = "src/main.rs"

[dependencies]
serde_json = "1"

[profile.release]
opt-level = 2
lto = true
strip = true
codegen-units = 1
